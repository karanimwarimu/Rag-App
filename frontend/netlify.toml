[build]
  base = "."
  command = "npm install && npm run build"
  publish = "dist/"

[build.environment]
  NODE_VERSION = "20"

# VITE_API_BASE_URL must be set in the Netlify build environment variables
# (it is inlined at build time, not read at runtime).
